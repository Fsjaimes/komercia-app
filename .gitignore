# Laravel
/vendor/
/.env
/.phpunit.result.cache
/storage/*.key
/storage/debugbar
/storage/app/public
/storage/framework/cache
/storage/framework/sessions
/storage/framework/views
/public/storage
/public/hot
/public/mix-manifest.json

# Node modules (Vue + Inertia, etc.)
node_modules/

# NPM/Yarn logs
npm-debug.log
yarn-error.log

# NPM package lock files (if you prefer not to commit them)
package-lock.json
yarn.lock

# IDE and editor files (optional, but recommended to avoid personal configurations)
.idea/
.vscode/

# Composer
/composer.lock

# Environment-specific files (for local, testing, or production environments)
.env.local
.env.testing
.env.production

# Logs
*.log

# Vite (if used for Laravel + Vue setup with Inertia)
/.vite/

# OS-specific files
.DS_Store
Thumbs.db
